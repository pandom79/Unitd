- Init process development

    - If the unit name doesn't exist then we could already block it client side.
      Call the daemon is useless even if the check must be done server side as well.

    - For the hibernate/suspend management we will use 'zzz' script gently provided by Leah Neukirchen

    - Review initialization and finalization scripts.
      Predict unitddatapath for the "run" property in the initial and final units.
      Replace it with unitddatapath macro value from meson

- Unitlogd development

- Unitlogctl development


