- Init process development
    - Show the error when we cannot enable unit because the wantedBy property doesn't match
      (default/cmdline, reboot or poweroff state)

    - Unitctl analyze command. We can use "unitctl list" changing the source data.
      Predict a bool argument (isAnalyze or analyze).

    - For the hibernate/suspend management we will use 'zzz' script gently provided by Leah Neukirchen

- Unitlogd development

- Unitlogctl development


